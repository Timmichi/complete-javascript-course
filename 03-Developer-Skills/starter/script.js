// Remember, we're gonna use strict mode in all scripts now!
// this just makes it so it displays all errors (no hidden)
'use strict';

// Click "Go live" at the bottom of vscode to run liveserver which auto-refreshes the browser
// with node.js we can just type "live-server" into the console.
console.log('hi');
